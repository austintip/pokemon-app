const DisplayCard = (props) => {
    return (
        <div className="pokemon-card">
            <h1>Hello! Pokemon will be here!</h1>
            {/* <img
            src={props.pokemon['image_url']}
            alt={`Picture of ${props.pokemon.name}`}
            />
            <p>
                {props.pokemon.name}
            </p> */}
        </div>
    );
}

export default DisplayCard;